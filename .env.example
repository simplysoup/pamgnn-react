# SMTP (Mailhog for local development)
SMTP_HOST=localhost
# For the Compose stack, use:
# SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=hello@pamgraphicdesign.com

# Next.js
NODE_ENV=development
NEXT_TELEMETRY_DISABLED=1
